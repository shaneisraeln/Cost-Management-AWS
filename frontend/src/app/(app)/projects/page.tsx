import { PagePlaceholder } from "@/components/PagePlaceholder";

export default function ProjectsPage() {
  return (
    <PagePlaceholder
      title="Projects"
      description="Spending grouped by project and environment, with budgets and attribution."
      phase="Phase 4 — Attribution"
    />
  );
}
